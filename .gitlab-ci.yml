stages:          # List of stages for jobs, and their order of execution
    - build
    - release

build-job:
    stage: build
    tags:
        - build-product
    script:
        - pwn

release-product:
    stage: release
    tags:
        - build-product
    script:
        - pwn


